<div id="confirmed-email" v-cloak>
  <div class="container">
    <h3 class="text-center"><%= __('Confirmation of email address completed') %></h3>
    <div class="confirmation-message">
      <hr />
      <p>
        <%= __('Congratulations! Your account has been verified. You now have full access to your account') %>
      </p>
      <p><a class="btn btn-elegant" href="/"><%= __('Main page') %></a></p>
    </div>
  </div>
</div>
<%- /* Expose locals as `window.SAILS_LOCALS` :: */ exposeLocalsToBrowser() %>